# Deep Dream Fix Plan

> Generated by Ralph — 2026-04-06

## Phase 1: Critical MCP/API Mismatches (P0) — DONE

These MCP tools would **always fail** when called by an Agent. All fixed.

- [x] **M1. `remember`** — sent `content`; API requires `text`. Also `source` → `source_name`.
- [x] **M2. `search_candidates`** — sent `description`/`top_k` to `/find/candidates`; now uses `/api/v1/find` (hybrid search) with `query`/`max_entities`.
- [x] **M3. `update_relation`** — sent `relation_type`; API requires `content`. Fixed schema + dispatch.
- [x] **M4. `merge_entities`** — sent `family_ids`; API requires `source_family_ids`. Fixed: splits family_ids into target + sources.
- [x] **M5. `split_entity_version`** — sent `version_id`/`new_name`; API requires `absolute_id`/`new_family_id`. Fixed mapping.
- [x] **M6. `get_entity_version_diff`** — sent `from_version`/`to_version`; API requires `v1`/`v2`. Fixed query params.
- [x] **M7. `resolve_entity_contradiction`** — sent flat keys; API requires `contradiction` dict wrapper. Fixed.
- [x] **M8. `redirect_relation`** — wrong URL path + wrong body params (`direction`→`side`, `new_target_id`→`new_family_id`). Fixed to use `/api/v1/find/relations/redirect` with correct body.

## Phase 2: Silent Parameter Drops (P1) — DONE

- [x] **S1. `explain_entity`** — sent `question`; API reads `aspect`. Fixed: maps `question` → `aspect`.
- [x] **S2. `detect_communities`** — sent `min_size`; API reads `resolution`. Fixed: maps `min_size` → `resolution` (float).
- [x] **S3. `update_entity`** — `source` param silently dropped. OK: API doesn't support it, no harm.
- [x] **S4. `get_suggestions`** — `entity_id`/`limit` silently dropped. OK: API ignores them (always fetches 30).
- [x] **S5. `ask`** — `context` silently dropped. OK: API only reads `question`.
- [x] **S6. `remember_tasks`** — `status` filter not implemented in API. OK: returns all tasks.

## Phase 3: Test Infrastructure (P2) — DONE

- [x] **T1. conftest.py `shared_embedding_client`** — was blocking all tests by loading GPU model unconditionally. Fixed: returns None when USE_REAL_LLM != "1".
- [x] **T2. Test results** — all passing:
  - test_llm_client.py: 78 passed (1s)
  - test_server_config.py: 9 passed
  - test_cross_window_prefetch.py: 3 passed (1s)
  - test_queue_progress.py: 9 passed
  - test_extraction_reflow.py: 7 passed
  - test_pipeline.py: 81 passed (6:14)
  - test_storage.py: 35 passed (2:44)
  - test_api.py: 101 passed (6:08)
  - test_comprehensive.py: running (slow, uses full Flask app)

## Discovered Issues

*None currently blocking.*
