"""
V2 Prompt Templates for the redesigned Remember pipeline.

Design principles:
- One task per prompt
- Short, clear instructions suitable for Qwen2.5-32B
- Simple JSON output formats
- Multi-round, multi-focus extraction for completeness
"""

# ============================================================
# Step 2a: Named Entity Extraction (concrete/named entities)
# ============================================================

ENTITY_NAMES_EXTRACT_NAMED_SYSTEM = """你是具名实体提取专家。从文本中找出所有具体的、有名字的实体。
宁可多提取也不要遗漏。"""

ENTITY_NAMES_EXTRACT_NAMED_USER = """从以下文本中列出所有具名实体。

重点提取：
- 人名：必须提取完整人名。如果文本中出现"庙号/谥号+人名"的形式（如"唐太宗李世民""诗仙李白"），要同时提取庙号/谥号和完整人名作为两个实体（如"唐太宗"和"李世民"都要提取）。不要只提取头衔而遗漏真实姓名。
- 地名（国家、城市、具体地点、建筑、机构地址）
- 组织名（公司、机构、学校、政府部门、研究所）
- 作品名（书名、电影名、产品名、系统名、项目名）
- 技术/工具名（编程语言、框架、协议、算法、库名）
- 具体物质/物种/器件/设备名
- 属性/特征/状态：文本中提到的具体属性名称、特征描述词、状态名称（如"content属性""版本号""主键"）

特别注意：文本的主题或核心对象本身也是一个实体，必须提取。如果文本讨论的是一个系统或流程中的具体概念（如"episode""版本""合并"），这些概念名称也要作为实体提取。

排除：
- 动词短语、句子片段
- 系统元数据（如"处理进度""步骤"）
- 文档元数据（如"章节标题""回目标题""列表主题""文档名"——这些是结构性标签，不是知识实体）
- 代词（他、她、它）

文本：
{window_text}

只输出一个```json```代码块，内部是字符串数组：
```json
["实体A", "实体B"]
```"""

# ============================================================
# Step 2b: Abstract Concept Extraction
# ============================================================

ENTITY_NAMES_EXTRACT_ABSTRACT_SYSTEM = """你是抽象概念提取专家。从文本中找出传统NER容易遗漏的概念、理论、方法和制度。"""

ENTITY_NAMES_EXTRACT_ABSTRACT_USER = """从以下文本中列出所有值得记忆的抽象概念。

重点提取：
- 理论、方法、原则、定律、学说（如"贞观之治""PageRank算法""相对论"）
- 时间概念：朝代、时期、时代（如"开元盛世""文艺复兴""二战期间"）
- 制度、政策、模式、标准（如"科举制度""B2B模式""开源协议"）
- 技术范式、设计思想（如"CSP并发模型""鸭子类型""垃圾回收"）
- 学科领域、研究方向（如"量子纠错""自然语言处理""红学"）
- 文化概念、哲学流派（如"浪漫主义""存在主义"）
- 设计模式、架构模式（如"版本控制""增量更新""对齐策略"）
- 关系类型本身：文本中描述的实体间关系的类型（如"从属关系""因果链"）
- 流程/步骤概念：文本中描述的处理流程的名称（如"概念抽取""概念对齐""版本溯源"）
- 规则/逻辑：文本中提到的规则、条件判断（如"LLM判断""一一对应"）

特别注意：如果文本在讨论一个系统或技术方案，其中涉及的每个操作概念、判断标准、处理策略都是值得提取的抽象概念。

排除：
- 已经是具体人名/地名/组织名的实体
- 孤立的、在文中无具体所指的泛化词（如单独的"文化""社会"）
- 系统元数据
- 文档元数据（"章节标题""回目标题""列表主题""文档名"等结构性标签）

文本：
{window_text}

只输出一个```json```代码块，内部是字符串数组：
```json
["概念A", "概念B"]
```"""

# ============================================================
# Step 2c: Event / Process Extraction
# ============================================================

ENTITY_NAMES_EXTRACT_EVENTS_SYSTEM = """你是事件提取专家。从文本中找出所有值得记忆的事件、过程和里程碑。"""

ENTITY_NAMES_EXTRACT_EVENTS_USER = """从以下文本中列出所有值得记忆的事件和过程。

重点提取：
- 历史事件（战役、革命、改革、危机，如"安史之乱""鸦片战争"）
- 里程碑事件（首次、创立、发布、获奖，如"阿里巴巴上市""量子霸权实验"）
- 决策/转折点（如"曹家被抄家""ARPANET切换到TCP/IP"）
- 持续性过程（如"汽车电动化转型""互联网商业化"）
- 操作/动作序列：文本中描述的一系列操作步骤（如"数据迁移""版本更新""内容合并"）
- 假设场景中的事件：举例说明中的事件（如"阅读小说时按500字分块"）

排除：
- 已经是具体人名/地名/组织名的实体
- 系统元数据

如果文本中没有明确的事件，输出空数组。

文本：
{window_text}

只输出一个```json```代码块，内部是字符串数组：
```json
["事件A", "事件B"]
```"""

# ============================================================
# Step 2d: Coverage Gap Check (补充提取轮)
# ============================================================

ENTITY_NAMES_SUPPLEMENT_SYSTEM = """你是实体名称查漏专家。检查已提取的实体列表，找出遗漏的实体。"""

ENTITY_NAMES_SUPPLEMENT_USER = """以下文本中已经提取了这些实体：
{existing_names}

请再次仔细阅读文本，找出还有哪些值得记住的实体被遗漏了。

重点关注：
- 文本中直接提到但不在已有列表中的人名、地名、组织名
- 被忽略的具体事物（产品、技术、事件等）
- 概念性实体（理论、方法、原则等）
- 间接提到的实体（通过代词或描述指代的对象）

不要重复已有列表中的实体。如果没有遗漏，输出空数组。

文本：
{window_text}

只输出一个```json```代码块，内部是新增的实体名称数组：
```json
["新实体A", "新实体B"]
```"""

# ============================================================
# Multi-round Continuation Prompts (for entity extraction)
# ============================================================

ENTITY_NAMES_EXTRACT_NAMED_CONTINUE = (
    "继续从文本中补充遗漏的具名实体。注意：\n"
    "1. 检查是否遗漏了文本中直接提到的人名、地名、组织名、作品名、技术/工具名\n"
    "2. 特别关注：被代词指代但未提取的对象、含简称的全称、含别名的实体\n"
    "3. 不要重复已提取的实体\n\n"
    "只输出```json```数组："
)

ENTITY_NAMES_EXTRACT_ABSTRACT_CONTINUE = (
    "继续从文本中补充遗漏的抽象概念。注意：\n"
    "1. 检查是否遗漏了理论、方法、原则、模式、流程、规则\n"
    "2. 特别关注：文本中隐含但未明确命名的概念、操作步骤名称、设计决策\n"
    "3. 不要重复已提取的概念\n\n"
    "只输出```json```数组："
)

ENTITY_NAMES_EXTRACT_EVENTS_CONTINUE = (
    "继续从文本中补充遗漏的事件和过程。注意：\n"
    "1. 检查是否遗漏了描述中的动作序列、状态变化、决策过程\n"
    "2. 特别关注：假设场景中的事件、因果关系中的过程\n"
    "3. 不要重复已提取的事件\n\n"
    "只输出```json```数组："
)

# ============================================================
# Step 2e: Reflection / Self-Critique (structured diagnostic)
# ============================================================

ENTITY_NAMES_REFLECTION_SYSTEM = """你是实体提取质量审查专家。通过结构化自检来发现遗漏的实体。"""

ENTITY_NAMES_REFLECTION_USER = """已完成以下实体提取。请通过以下诊断问题自检是否还有遗漏：

已提取实体：{existing_names}

请逐项检查：
1. 【核心主题】文本讨论的核心主题或主要对象是否已提取？
2. 【属性/特征/状态】文本中提到的具体属性名、特征描述词、状态名称是否已提取？（如"content属性""版本号""增量更新"）
3. 【因果链概念】文本中描述的因果链中的每个环节概念是否已提取？（如"概念抽取→对齐→判断→合并"中的每一步）
4. 【对比对象】如果文本中存在对比（如"第一种情况 vs 第二种情况"），对比双方是否都已提取？
5. 【隐含引用】文本中通过代词或描述间接引用的对象是否已解析并提取？
6. 【举例概念】文本中举例说明时提到的概念是否已提取？（如"小说""500字切分"）

如果发现遗漏，输出遗漏的实体名称。如果以上都已覆盖，输出空数组。

文本：
{window_text}

只输出一个```json```代码块，内部是新增的实体名称数组：
```json
["遗漏实体A", "遗漏实体B"]
```"""

# ============================================================
# Step 4: Per-Entity Content Writing (one call per entity)
# ============================================================

ENTITY_CONTENT_WRITE_SYSTEM = """你是知识描述专家。根据文本为指定实体撰写简洁准确的描述。
你必须输出JSON格式，不要输出纯文本。"""

ENTITY_CONTENT_WRITE_USER = """根据以下文本，描述实体"{entity_name}"。

要求：
1. 只描述该实体本身，不描述与其他实体的关系
2. 包含：它是什么、有什么特征、在文本中的角色
3. 如果文本提到了该实体的时间、地点、数量等具体信息，要包含进去
4. 30到100字，简洁准确
5. 不要用"该实体""这是一个"等模板化开头
6. **禁止**直接复制文本中的Markdown标题行（如"## 标题"、"### 标题"、"#### 标题"）作为描述内容
7. **禁止**直接引用包含emoji或特殊符号的格式行（如"✅"、"👉"、"🧠"、"1️⃣"开头的行）
8. 用自己的语言概括，不要照搬原文

文本：
{window_text}

只输出一个```json```代码块，不要输出其他任何文字：
```json
{{"content": "描述内容"}}
```"""

# ============================================================
# Step 6a: Relation Pair Discovery (window-level)
# ============================================================

RELATION_PAIRS_DISCOVER_SYSTEM = """你是关系发现专家。从文本中找出实体间的关联。宁可多发现也不要遗漏。
重要：你必须在两个具体的命名实体之间发现关系，而不是在实体和其所属类别之间。"""

RELATION_PAIRS_DISCOVER_USER = """给定以下实体列表，从文本中找出哪些实体对之间存在关联。

关联标准（宽松但有意义）：
- ✅ 使用、包含、从属、交互、创建、影响、合作、组成、竞争、因果关系
- ✅ 人与组织的归属关系、人与事件的关系、事件与结果的关系
- ✅ 在同一事件/场景中被共同提及且存在语义联系
- ✅ 对比关系、并列关系（如两个被比较的人物或技术）
- ✅ 技术/工具之间的搭配使用关系（如Kubernetes与Helm、PostgreSQL与Redis）
- ✅ 同一系统/架构中各组件之间的协作关系（如主数据库+缓存、消息队列+微服务）
- ✅ 人物与其经历/到达的地点之间的关系（如玄奘到达天竺、李世民定都长安）
- ✅ 因果关系链：A导致B、A是B的原因（如"概念对齐→发现已有概念→版本更新"）
- ✅ 组成/层级关系：A是B的组成部分、A属于B的子类别（如"episode→概念抽取→概念版本"）
- ✅ 属性归属关系：A是B的属性/特征（如"content→概念版本的属性"）
- ❌ 纯粹巧合出现在同一段落、完全无语义联系
- ❌ 实体与其自身所属类别的关系（如"Kubernetes"与"容器编排"、"Redis"与"数据库"）

注意：
- 尽可能多地发现关联，不要遗漏
- 特别注意人物与其所属组织/创立的公司/发明的技术之间的关系
- 特别注意人物之间的关系（师生、合作、竞争、对比）
- 特别注意技术/工具之间的搭配和协作关系（例如：容器编排工具+包管理工具、主数据库+缓存、编程语言+通信框架）
- 特别注意人物与地点的关系（旅行、出发、到达、定都等）
- 当文本提到多个技术在同一架构中搭配使用时，必须把搭配的技术两两配对（例如"主库用PostgreSQL，缓存用Redis"→必须提取["PostgreSQL","Redis"]这一对）
- 不要把两个具体实体分别与某个抽象类别关联，而是直接发现它们之间的关系

实体列表：{entity_names}

文本：
{window_text}

只输出一个```json```代码块，内部是实体对的数组，每对是一个两个元素的数组：
```json
[["实体A", "实体B"], ["实体C", "实体D"]]
```"""

# ============================================================
# Step 6b: Relation Pair Expansion (find missed pairs)
# ============================================================

RELATION_PAIRS_EXPAND_SYSTEM = """你是关系查漏专家。检查已发现的关系对，找出被遗漏的关联。
重要：只在两个具体的命名实体之间发现关系，不要发现实体与其所属类别之间的关系。"""

RELATION_PAIRS_EXPAND_USER = """以下实体之间已经发现了这些关系对：
{existing_pairs}

请再次仔细阅读文本和实体列表，找出还有哪些有意义的关联被遗漏了。

重点关注：
- 人物与其所属组织/创立的公司/发明的技术之间是否有遗漏
- 人物之间的直接关系（师生、夫妻、合作伙伴、竞争对手）
- 事件与参与者之间的关系
- 技术/工具之间的搭配使用和协作关系（如容器编排+包管理、数据库+缓存）
- 技术之间的依赖/替代/组合关系
- 对比关系（文本中被比较的对象）
- 同一场景中共同使用的工具/技术之间的关联

不要重复已有的关系对。如果没有遗漏，输出空数组。

实体列表：{entity_names}

文本：
{window_text}

只输出一个```json```代码块，内部是新发现的实体对数组：
```json
[["实体A", "实体B"]]
```"""

# ============================================================
# Step 6c: Orphan Entity Relation Recovery
# ============================================================

RELATION_ORPHAN_RECOVERY_SYSTEM = """你是关系发现专家。以下实体在文本中还没有发现任何关系，请为它们寻找关联。"""

RELATION_ORPHAN_RECOVERY_USER = """以下实体在文本中尚未发现任何关系，请为它们找出与其他实体之间的关联：

孤立实体：{orphan_names}
所有已知实体：{all_entity_names}

请仔细阅读文本，找出这些孤立实体与其他实体（不限于孤立列表中）之间的关联。

关注：
- 孤立实体与文本中其他实体的使用、包含、从属、因果关系
- 孤立实体在同一场景/过程/流程中与其他实体的协作关系
- 孤立实体的属性、特征与其他实体的关联

如果某个孤立实体确实与所有其他实体都没有有意义的关系，可以跳过。

文本：
{window_text}

只输出一个```json```代码块，内部是实体对数组：
```json
[["实体A", "实体B"]]
```"""

# ============================================================
# Step 7: Per-Pair Relation Content Writing (one call per pair)
# ============================================================

RELATION_CONTENT_WRITE_SYSTEM = """你是关系描述专家。用自然语言描述两个实体间的具体关联。
你必须输出JSON格式，不要输出纯文本。"""

RELATION_CONTENT_WRITE_USER = """根据以下文本，描述"{entity_a}"和"{entity_b}"之间的关系。

要求：
1. 具体描述关联内容（10到50字）
2. 不要写泛泛的"有关联""存在关系"
3. 描述必须与两个端点实体直接相关
4. 包含具体的关联类型（如"创立""发明""属于""影响了"）

文本：
{window_text}

只输出一个```json```代码块，不要输出其他任何文字：
```json
{{"content": "关系描述"}}
```"""

# ============================================================
# Step 9: Entity Alignment Judgment (three-way: same/different/uncertain)
# ============================================================

ENTITY_ALIGNMENT_JUDGE_V2_SYSTEM = """判断两个实体是否描述同一个对象。你需要给出明确的判断和置信度。
如果不确定，请选择"uncertain"——宁可不合并也不要错误合并。"""

ENTITY_ALIGNMENT_JUDGE_V2_USER = """实体A（新抽取）: "{name_a}"
内容摘要: {content_a}

实体B（已有）: "{name_b}"
内容摘要: {content_b}

{name_relationship}判断它们是否描述同一个对象。

判断标准：
- same: 确定是同一个对象（同一个人的全名/简称/别名、同一组织的全称/简称、同一概念的中英文）
- different: 确定是不同的对象（不同的人、不同的概念、父子/上下级关系）
- uncertain: 可能相同但无法确定（信息不足、存在歧义）

以下情况请选择 different：
- 人 vs 其作品（曹雪芹 vs 红楼梦）
- 组织 vs 其产品（Google vs Android）
- 父子/上下级（阿里巴巴 vs 淘宝）
- 相似但不同概念（人工智能 vs 机器学习）

以下情况请选择 uncertain：
- 同名但无法判断是否同一个（如两个"张伟"）
- 信息不足以判断

只输出一个```json```代码块：
```json
{{"verdict": "same", "confidence": 0.9, "reason": "简短原因"}}
```"""

# ============================================================
# Step 10: Relation Alignment Judgment (when existing relations found)
# ============================================================

RELATION_ALIGNMENT_JUDGE_SYSTEM = """判断新关系是否与已有关系重复。"""

RELATION_ALIGNMENT_JUDGE_USER = """新关系：
实体："{entity_a}" 和 "{entity_b}"
描述：{new_content}

已有关系：
{existing_relations_text}

新关系是否与已有某条关系描述的是同一件事？
如果是，输出匹配的索引（从0开始）。
如果是不同的关系，输出-1。

只输出一个```json```代码块：
```json
{{"match_index": 0}}
```
或
```json
{{"match_index": -1}}
```"""
