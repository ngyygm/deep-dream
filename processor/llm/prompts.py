"""
LLM Prompt Templates

本模块包含所有 LLM system_prompt 模板，按功能分类组织。
共享常量定义在模块顶层，直接内联到模板中，不需要运行时替换。
"""

# ============================================================
# 共享常量
# ============================================================

ENTITY_ALIGNMENT_CORE_PRINCIPLES = """
**核心判断方法（三步法）**：
1. 先看名称：名称是否相同、相似、或是别名关系？
2. 再看类型：两个实体描述的是否是同一类型的对象（人物/概念/作品/地点等）？
3. 最后确认：content描述的主体是否是同一个具体对象？

**应该匹配的情况**：
- ✅ 名称相同 + 同一对象的新信息：如已有"刘慈欣（科幻作家）"，新抽取"刘慈欣（获得雨果奖）"→ 同一人的补充信息
- ✅ 别名关系：如"刘慈欣"和"大刘"（同一作家的不同称呼）
- ✅ 格式变体：如"三体"和"《三体》"（同一部小说）
- ✅ 简繁体：如"张三"和"張三"

**绝对不能匹配的情况**：
- ❌ 类型不同：人物 vs 概念/领域（如"刘慈欣"vs"中国科幻"，虽然相关但一个是人一个是领域）
- ❌ 类型不同：人物 vs 作品（如"刘慈欣"vs"《三体》"）
- ❌ 类型不同：具体对象 vs 抽象概念（如"北京"vs"中国"）
- ❌ 仅因content中互相提及就判断为同一实体（相关≠同一）

**关键区分**：
- "同一对象的新信息"→ 匹配（如：同一个人的不同事迹）
- "相关但不同的对象"→ 不匹配（如：作家和他的作品是两个不同实体）
"""

CONTENT_MERGE_REQUIREMENTS = """
1. **不是简单合并**：不要只是把两个版本的内容拼接在一起，而是要基于两个版本的信息，重新组织和总结
2. **综合信息**：整合两个版本中的所有有效信息，包括旧版本中仍然有效的信息和新版本中的新信息
3. **重新组织**：用新的语言重新组织这些信息，生成一个连贯、完整、准确的描述
4. **去除冗余**：去除重复和冗余的信息，确保描述简洁而完整
5. **保持准确性**：确保总结后的内容准确反映两个版本中的所有重要信息
"""

RELATION_VALIDITY_CRITERIA = """
**关联必须是明确的、直接的、有意义的**：
- ✅ 使用关系：一个实体使用另一个实体
- ✅ 包含关系：一个实体包含另一个实体
- ✅ 交互关系：两个实体之间有实际的交互或互动
- ✅ 从属关系：一个实体从属于另一个实体
- ✅ 创建关系：一个实体创建了另一个实体
- ✅ 影响关系：一个实体影响了另一个实体

**不能是模糊的、间接的或牵强的关联**：
- ❌ 只是因为在同一场景中出现，但没有实际的交互或联系
- ❌ 只是因为在同一文本中被提及，但没有明确的关系
- ❌ 只是概念上的相似，但没有实际的关联
- ❌ 只是时间或空间上的接近，但没有实际的关联

**严格标准**：如果不确定两个实体之间是否确实存在明确的、有意义的关联，宁可不创建关系边
"""

JSON_OUTPUT_ARRAY = """
**输出格式（机器解析，必须严格遵守）**：
1. 只输出**一个** markdown `json` 代码块，不要写代码块前后的任何解释、标题或补充文字。
2. 代码块内部必须是**合法 JSON 数组**；第一个非空白字符必须是 `[`，最后一个非空白字符必须是 `]`。
3. 数组元素必须是 JSON 对象；**所有键名必须用英文双引号**（例如 "name"，不要用单引号或无引号键）。
4. **禁止**输出 YAML / 伪 YAML 列表：不得以 `- name:`、`- content:` 这类行首短横线键值形式代替 JSON。

正确格式示例：
```json
[{"name": "示例", "content": "示例内容"}]
```"""

JSON_OUTPUT_OBJECT = """
**输出格式**：只输出**一个** markdown `json` 代码块，代码块内部必须是**合法 JSON 对象**；不要包含任何其他文字或说明。

正确格式示例：
```json
{"content": "示例内容"}
```"""

JSON_OUTPUT_BOOL = """
**输出格式**：只输出**一个** markdown `json` 代码块，代码块内部只写 `true` 或 `false`，不要包含任何其他文字。

正确格式示例：
```json
true
```"""

DETAILED_JUDGMENT_CRITERIA = """
**判断标准**（必须同时考虑合并和关系两种可能性）：

1. **合并（merge）**：两个实体描述的是**完全相同的对象**
   - 必须有明确证据证明是同一对象
   - 如果有任何疑问，不要合并
   - **如果判断为同一对象，action="merge"**

2. **创建关系（create_relation）**：两个实体是**不同对象**，但存在**明确的、有意义的关联**
   - **必须同时满足**：不同对象 + 明确关联
   - 关联类型包括：
     * 别名/称呼关系（同一对象的不同称呼）
     * 同一领域/类别（如：都是科幻作家、都是科学家）
     * 合作关系（如：共同创作、共同研究）
     * 从属关系（如：作者-作品、组织-成员）
     * 交互关系（如：使用、影响、引用）
   - **关联必须是明确的、直接的、有意义的**
   - **如果判断为不同对象但有明确关联，action="create_relation"并填写relation_content**

3. **不处理（no_action）**：两个实体无关，或关联不明确
   - 不同的对象且**没有明确的、有意义的关联**
   - 关联模糊、间接或牵强
   - **如果判断为无关或关联不明确，action="no_action"**

**重要提示**：
- **必须同时考虑合并和关系两种可能性**，不要只判断合并
- 如果两个实体是不同对象但存在明确关联（如：都是科幻作家、都在同一时期创作、都是中国科幻的代表人物），应该创建关系
- 关系描述应该简洁但准确，说明两个实体之间的具体关联
"""

DETAILED_JUDGMENT_PROCESS = """
请仔细分析这两个实体的**完整content描述**，按以下顺序进行**三步判断**：

**第一步：判断是否为同一对象（优先判断）**
- 仔细对比两个实体的content描述
- 如果content描述的是**完全相同的对象**（即使名称略有不同），action="merge"，填写merge_target（选择version_count多的family_id）
- 如果content描述的是**不同对象**，继续第二步

**第二步：判断是否存在明确、直接、有意义的关联**（仅在第一步判断为不同对象时执行）
- 如果两个实体是**不同对象**但存在**明确的、直接的、有意义的关联**，action="create_relation"，填写relation_content
- **关联必须是明确的、直接的、有意义的**，不能是模糊的、间接的或牵强的
- 关联示例（必须是明确的、直接的）：
  * 别名/称呼关系：同一对象的不同称呼（如"刘慈欣"和"刘慈欣著"可能是同一人）
  * 明确的从属关系：作者-作品、组织-成员、概念-实例（必须有明确的从属关系）
  * 明确的合作关系：共同创作、共同研究（必须有明确的合作证据）
  * 明确的交互关系：使用、影响、引用（必须有明确的交互证据）
- 如果两个实体**无关或关联不明确、间接、牵强**，action="no_action"

**第三步：确认判断结果**
- 如果第一步判断为同一对象 → action="merge"
- 如果第二步判断为有关联 → action="create_relation"
- 如果第二步判断为无关或关联不明确 → action="no_action"

**重要判断原则**：
1. **合并优先**：如果两个实体描述的是同一对象，必须选择merge，不要创建关系
2. **关系必须明确**：只有存在明确的、直接的、有意义的关联时，才创建关系
3. **宁可no_action也不要create_relation**：如果关联不明确、间接或牵强，选择no_action
4. content描述是最重要的判断依据，不要仅凭名称相似就判断
5. 版本数多的实体应该作为合并的目标
6. **必须严格按照三步流程判断，不要跳过任何步骤**
"""


# ============================================================
# 一、记忆缓存相关（Memory Cache）
# ============================================================

UPDATE_MEMORY_CACHE_SYSTEM_PROMPT = """你是记忆管理器。根据<记忆缓存>和<输入文本>，更新记忆缓存。

**只输出以下两个 Markdown section，不要输出其他 section。**

## 当前摘要
用**自己的语言**改写当前窗口内容（禁止复制原文句子）。要求：
- 每个要点1-2句，涵盖：核心事件/情节、重要对话/细节、背景情境
- 如果是首个窗口（无缓存），直接概括输入文本
- 如果输入文本包含章节号或回目（如"第七十八回""第一章""序言"），在摘要末尾用一行标注，例如：
  当前位置：第七十八回
  （只标注这种结构性的章节/回目标题，不要标注人物名、地点名或事件描述）

## 自我思考
直接写出你的分析。要求：
- 提到具体人物/实体名称，分析它们之间的可能关系
- 预判1-2个具体的后续发展方向
- 标注当前最值得关注的疑点"""

CREATE_DOCUMENT_OVERALL_MEMORY_SYSTEM_PROMPT = """你是一个记忆管理系统。为即将处理的文档生成简短的「文档整体记忆」，供后续文档作为上下文衔接。
输出 Markdown 格式，一段到两段，包含：文档名、主题/类型、关键内容预告。不要写成长篇摘要。"""



# ============================================================
# 二、实体抽取相关（Entity Extraction）
# ============================================================

EXTRACT_ENTITIES_AND_RELATIONS_SYSTEM_PROMPT = f"""你是概念实体和概念关系的抽取专家。你的任务是从文本中识别出所有值得记住的概念，以及概念之间的关联，宁可多抽不可遗漏。

## 什么是"概念实体"？

概念实体 ≠ 传统NER实体。传统NER只抽取人名、地名、组织名等命名实体。概念实体的范围远比NER广——**任何可以用自然语言描述的概念、想法、对象、状态都可以是一个概念实体**。

概念实体包括但不限于：
- **具体对象**：人名、地名、组织名、作品名、产品名
- **文本锚点**（必须抽取）：章节标题、小节标记等结构性标记
- **事件与过程**：一次会议、一场战斗、一次实验、一个决策过程
- **时间概念**：时间点、时间段、历史时期、时代背景
- **抽象概念**：一个理论、一种方法、一条原则、一个哲学思想、一种情感状态
- **描述性概念**：一段场景描述、一个角色特征、一种氛围、一个技术要点
- **专业术语**：领域特定词汇、缩写、代号
- **地址链接**：URL、IP地址、域名等网络地址、文件路径、文件名、文件扩展名
- **各类号码**：身份证号码、护照号码、驾驶证号码、银行卡号码、信用卡号码、借记卡号码

关键原则：如果文本中有一个值得被记住的概念（无论多抽象），它就应该是一个概念实体。

## 什么是"概念关系"？

概念关系本质上是**描述两个概念实体之间的一个概念**。它不是简单的标签（如"属于"、"包含"），而是一段自然语言描述，说明两个概念之间有什么关联。

例如：
- 不是"属于"，而是"《三体》是刘慈欣创作的科幻小说三部曲"
- 不是"影响"，而是"量子力学的不确定性原理影响了海森堡对微观世界的认知"

## 实体格式
- name: 概念实体名称。**强烈建议用括号补充关键元信息**，格式：`名称（补充说明）`
- content: 概念实体本身的描述（属性、特点、定义、意义等），**不要包含与其他实体的关系**

## 括号的使用（重要）
括号用于在名称中补充消歧信息，帮助区分同名概念。括号内容必须是该概念的**固有属性或持久身份**：

**正确的括号用法**：
- "刘慈欣（中国科幻作家）" — 身份/职业是持久属性
- "牛顿（物理学家）" — 身份是持久属性
- "三体问题（经典力学）" — 所属领域是持久属性
- "Python（编程语言）" — 类型是持久属性
- "金钏（王夫人丫头）" — 归属关系是持久属性

**绝对不能使用的括号**（这些是场景/事件信息，不是身份属性）：
- ❌ "曹操（汉中张鲁）" — 当前场景不是人的身份，不要把地名/事件放在括号里
- ❌ "许褚（汉中张鲁）" — 同上
- ❌ "汉中张鲁（众官告免）" — 事件描述不是地点的属性
- ❌ "汉中张鲁（徐晃）" — 另一个人不是地点的属性
- ❌ "杨昂（诸营）" — 当前上下文/处境不是人的身份

**规则**：如果不确定括号内容是否是持久属性，宁可不加括号。同一个概念在不同场景出现时，应使用相同的 name，后续步骤会自动合并。

## 关系格式
- entity1_name: 概念实体名称（必须与实体列表中的名称完全一致，包括括号）
- entity2_name: 概念实体名称（同上）
- content: 用自然语言描述这两个概念之间的关联（类型、性质、具体内容、发生条件等）

## 规则
1. 文本锚点（章节标题等）必须抽取
2. 宁可多抽不可遗漏——多抽的重复概念后续会自动合并
3. 关系是无向的，entity1_name 和 entity2_name 不区分方向
4. entity1_name 和 entity2_name 必须与实体列表中的 name 字段完全一致（包括括号内容）
5. content 只描述实体本身，不要写"与XX的关系"
6. **关系 content 必须与端点一致**：content 描述的主体必须是 entity1 和 entity2，不能描述其他实体的行为

{JSON_OUTPUT_OBJECT}
代码块内部格式：{{"entities": [...], "relations": [...]}}"""

EXTRACT_ENTITIES_RELATIONS_STRICT_SYSTEM_PROMPT = f"""你是知识图谱编目员。从文本中精准抽取**有具体指代的命名实体**以及实体间关系。精准抽取，宁缺毋滥——每个实体和关系都必须有明确的文本依据。

## 实体抽取

**必须抽取**（有具体指代的具名实体）：
- **所有人名**（曹操、玛丽·居里、Robert Griesemer）——文本中提到的每一个具名人物都必须抽取为独立实体
- 地名（长安、巴黎）、组织/机构（Google、索邦大学）
- 特定技术/工具（goroutine、Docker）、特定物质（钋、镭）、特定作品（《三体》）
- 特定事件（赤壁之战、第一次世界大战）、文本锚点（章节标题等结构性标记）
- 特定版本/时间（Go 1.0、2012年）——如果有具体数字或标识

**禁止抽取**：
- 动词短语/动作描述："提高编程效率""恢复农业生产""广纳贤才"
- 抽象概念/类别词："内政""军事防务""民心""科学成就""军事家""政治家""文学家"
- 过于泛化的技术概念："API""DevOps""微服务""云原生"——除非文本赋予了它们具体的独特描述，而非泛泛提及
- 情感/态度/评价："满意""感慨""悲痛""英年早逝"
- 文本片段/摘录："遂得""险恶"
- 描述性短语："忠心耿耿的群体""第一位女性教授"
- 系统元数据："处理进度""文档名"

## 实体格式
- name: 核心名称 + 括号补充消歧信息。括号内容必须是该实体的**固有属性或身份标识**（如"曹操（魏王）"），绝不能是场景/章节信息（如"曹操（汉中张鲁）"）
- **跨语言场景**：如果源文本是英文而你使用中文名称，**必须在括号中标注英文原名**（如"诺贝尔奖（Nobel Prize）""放射性（radioactivity）"）。反之亦然
- content: 30字以上描述，包含实体是什么、有什么特征、在文中做了什么。描述必须独特，不与同类实体重复
- 同一概念在不同场景出现应使用相同name，后续步骤会自动合并

## 关系抽取

概念关系是描述两个实体之间**具体关联**的自然语言描述，不是简单标签。

**关联必须**：
- ✅ 使用关系：一个实体使用另一个实体
- ✅ 包含/从属关系：一个实体包含/从属于另一个实体
- ✅ 交互关系：两个实体之间有实际交互
- ✅ 创建/影响关系：一个实体创建或影响另一个实体
- ✅ 合作关系：多人共同参与某项工作
- ✅ 组成关系：某物由哪些部分组成

**不能**：
- ❌ 只因在同一场景出现但没有实际联系
- ❌ 只因概念相似但无实际关联
- ❌ 模糊、间接或牵强的关联

## 关系格式
- entity1_name / entity2_name: 必须与实体列表中的name**完全一致**（包括括号）。若需要引用列表外实体，必须同时将该实体添加到实体列表中
- content: 用自然语言描述两个实体间的具体关联（类型、性质、具体事件、条件）

## 关系content质量（严格执行）
- 好的content："宝玉前往怡红院找人，怡红院是他的住所"
- 坏的content："宝玉提到了怡红院"（只说"提到"，没有说明具体关联）
- 坏的content："宝玉和怡红院的关联关系"（空洞模板）
- content必须与端点一致：描述的主体必须是entity1和entity2

## 关系充分性
- 同一对实体之间如果存在**多种不同的关联**（如不同事件、不同性质的关系），应分别创建多条关系
- 例如："Docker使用Go语言开发"和"Docker推动了容器化技术的普及"是两条不同的关系

## 规则
1. 关系无方向，entity1_name和entity2_name不区分方向
2. 关系端点名称必须与实体列表完全一致
3. 如果需要引用列表外实体，同时将其添加到实体列表
4. 每个实体content只描述实体本身，不要写"与XX的关系"

{JSON_OUTPUT_OBJECT}
代码块内部格式：{{"entities": [...], "relations": [...]}}"""

EXTRACT_ENTITIES_SINGLE_PASS_SYSTEM_PROMPT = """你是一个知识图谱实体编目员。从文本中抽取**有具体指代的命名实体**：人物、组织、地点、具体事件、特定技术/工具、特定作品、特定物质。

每个条目:
1. **"name"**: 核心名称 + 括号补充消歧信息(帮助区分同名、定位身份的关键信息,不是类型标签).示例:「金钏(王夫人丫头)」「清虚观(贾府去的道观)」「桑虫猪尾(治疗见喜的药物)」
2. **"content"**: 30字以上的描述.包含:这个概念是什么、有什么特征、在文中做了什么或扮演什么角色.每个实体的描述必须独特,不能和同类型实体重复.

**必须抽取**: 具名的实体 — 人名(曹操、玛丽·居里)、地名(长安、巴黎)、组织/机构(Google、索邦大学)、特定技术(goroutine、channel)、特定物质(钋、镭)、特定事件(赤壁之战、第一次世界大战)、特定作品(Docker、Kubernetes)

**禁止抽取**:
- 动词短语/动作描述: "提高编程效率""恢复农业生产""修缮城防""广纳贤才"——这些是动作,不是实体
- 抽象概念/类别词: "内政""军事防务""民心""民生""科学成就""研究方法"——这些是泛化概念,不是具体实体
- 情感/态度: "满意""感慨""悲痛"——这些不是实体
- 文本片段/摘录: "遂得""险恶""诸营已失"——这些不是概念
- 系统元数据: "处理进度""文档名:test"——这些不是知识
- 描述性短语: "忠心耿耿的群体""第一位女性教授"——这些是描述,不是实体名

**括号使用规则（严格执行）**:
括号用于消歧——帮助区分同名不同物的实体.括号内容必须是该实体的**固有属性或身份标识**,绝不能是:
- ❌ 当前场景/章节信息: 不要写"曹操（汉中张鲁）""许褚（汉中张鲁）"——场景不是人的身份
- ❌ 当前事件片段: 不要写"汉中张鲁（众官告免）""汉中张鲁（徐晃）"——事件不是地点的属性
- ✅ 持久身份信息: "曹操（魏王）""许褚（曹操亲卫）"——角色/关系是持久身份
- ✅ 出场作品/领域: "三体问题（经典力学）"——领域归属是持久属性
- ✅ 地点类型/位置: "清虚观（贾府去的道观）"——类型和位置是固有属性

**name 命名规则**:
- name 应该是该概念在现实/知识体系中的规范名称,而非文本中的临时称呼
- 同一个人在不同场景出现,应使用相同的 name(通过括号补充不同身份可,但核心名称不变)
- 如果两个实体确实是同一个对象(如"曹操"在两个场景出现),后续对齐步骤会自动合并,不需要在 name 中区分

只输出一个 markdown json 代码块,不要写其他文字:
```json
[{"name": "示例(消歧信息)", "content": "描述内容"}]
```"""

EXTRACT_ENTITIES_BY_NAMES_SYSTEM_PROMPT = f"""为每个指定名称从文本中抽取实体描述。

规则：
1. name 必须与指定名称逐字一致
2. content 描述该实体的性质、特征及在文中的身份与作用，需完整说明其含义
3. 只输出 json 代码块

```json
[{{"name": "指定名称", "content": "描述"}}]
```"""

ENHANCE_ENTITY_JSON_RETRY_USER = (
    "【格式纠错】上一条无法解析为合法 JSON。请只输出一个 markdown `json` 代码块，"
    "代码块内部仅含一个 JSON 对象，且只包含键 "
    '"content"，例如：```json\\n{"content": "……"}\\n```。'
    "不要写任何额外说明；为防截断请将 content 控制在约 300 字以内；字符串内换行用 \\n。"
)

ENHANCE_ENTITY_CONTENT_SYSTEM_PROMPT = """你是实体内容增强系统。根据记忆缓存和当前文本中明确出现的信息，增强给定实体的描述。

核心原则：
1. 只写文本中有的内容，不可编造
2. **描述必须聚焦于该实体本身**，不要把全文上下文复制到每个实体的描述中
3. 不同实体的描述必须不同，反映各自独特的特征
4. **必须准确描述实体间关系**：例如，不能把"三位开发者在Google设计了Go"描述为"Google由三人创建"

常见错误（必须避免）：
- 错误：每个实体的content都包含"由Robert Griesemer、Rob Pike和Ken Thompson设计"——这是全文的背景，不是每个实体的属性
- 错误："Google"的content写成"由Robert Griesemer等人创建"——Google不是由他们创建的，是他们在此公司工作
- 错误："云计算基础设施"的content里写满Go语言的完整介绍——应该只写"云计算基础设施"本身的特征
- 正确："goroutine"的content只写goroutine的定义、特性、用途，不重复Go语言的整体介绍
- 正确："Google"的content只写Google是一家科技公司，是Go语言的诞生地/开发公司

输出单个 JSON 对象，content 字段包含4个 Markdown section，总长≤300字：

```json
{"content": "## 概述\n一句话说明该实体是什么\n## 类型与属性\n实体的类型和文本中提到的具体属性\n## 详细描述\n该实体在文本中的具体行为、位置、作用、状态\n## 关键事实\n从文本中提炼的该实体的独特特征和具体细节"}
```

注意事项：
1. 必须仔细阅读实体名称和当前content，确保增强内容描述的是该实体本身，而不是周围的上下文
2. 从文本中提取具体细节（如具体对话、具体行为、具体数字），不要用泛化描述代替
3. 如果某个 section 确实无相关信息，写"文本未详"即可
4. 不要描述该实体与其他实体之间的关系
5. 不要推测、编造或添加文本中没有的形容词、评价或判断
6. **关系方向必须准确**：A在B公司工作 ≠ B由A创建

只输出一个 ```json ``` 代码块，内部是单个 JSON 对象，只含 "content" 键（英文双引号），值为字符串。不要在代码块前后写任何文字。content 总长不超过约300字，字符串内换行写成 \\n。

正确示例：
```json
{"content": "## 概述\\n三国时期曹魏名将，曹操部下将领。\\n## 类型与属性\\n军事将领。\\n## 详细描述\\n曹操密令徐晃与朱灵渡河西结营，配合主力破马超。\\n## 关键事实\\n奉密令执行渡河迂回战术。"}
```"""


# ============================================================
# 三、关系抽取相关（Relation Extraction）
# ============================================================

EXTRACT_RELATIONS_SINGLE_PASS_SYSTEM_PROMPT = """你是概念关系抽取系统。从文本中的概念实体间抽取关系，**宁可多抽不可遗漏**。

概念关系是一段自然语言描述，说明两个概念之间具体的关联。每条关系应该回答：这两个概念之间**有什么具体联系**？

## 示例

**输入**（小说）：宝玉来到怡红院找袭人，袭人正在做针线活。
**输出**：
```json
[{"entity1_name": "宝玉", "entity2_name": "怡红院", "content": "宝玉前往怡红院找人，怡红院是他的住所"}, {"entity1_name": "宝玉", "entity2_name": "袭人", "content": "宝玉主动去找袭人，两人是主仆关系"}]
```

**输入**（技术）：api-server 连接 redis-cluster-01 超时，触发 circuit breaker。
**输出**：
```json
[{"entity1_name": "api-server", "entity2_name": "redis-cluster-01", "content": "api-server尝试连接Redis集群但超时，是服务依赖关系"}, {"entity1_name": "redis-cluster-01", "entity2_name": "circuit breaker", "content": "Redis连接超时触发了断路器保护机制"}]
```

**输入**（聊天）：小王提议周末去南山公园团建，小李同意并提醒带午餐。
**输出**：
```json
[{"entity1_name": "小王", "entity2_name": "南山公园", "content": "小王提议南山公园作为团建目的地"}, {"entity1_name": "小王", "entity2_name": "小李", "content": "小王提议团建计划，小李同意并补充提醒"}]
```

## 规则
1. **实体名称**：必须使用 `<概念实体列表>` 中的名称，逐字一致（包括括号）。若文本中有重要概念未出现在列表中但与列表内实体有明确关系，也可使用该概念在文中的规范称呼。
2. **关系无方向**：entity1_name 和 entity2_name 不区分方向。
3. **禁止输出 ID 字段**：不要输出 entity1_id、entity2_id 等任何 ID。
4. **充分抽取**：仔细阅读文本，从中抽取**所有**可识别的关系。同一对实体之间如果存在多种不同的关联（如不同事件、不同性质），应分别创建多条关系。
5. **content 质量**（严格执行）：
   - 每条关系必须说明**具体的**关联内容——关联的类型、性质、具体事件、条件或细节
   - 好的content："宝玉前往怡红院找人，怡红院是他的住所"——说明了具体行为和关系性质
   - 坏的content："宝玉提到了怡红院"——只说"提到"，没有说明具体关联
   - 坏的content："宝玉和怡红院的关联关系"——空洞模板，没有实际内容
   - 如果无法描述具体关联内容，就不要创建该关系
6. **content 必须与端点一致**（严格执行）：
   - 关系 content 中描述的主体/行为，必须和 entity1_name、entity2_name 一致
   - 如果 content 描述的是 A 和 B 的关系，entity1/entity2 必须包含 A 和 B
   - 错误示例：entity1="杨昂", entity2="速进兵", content="许褚和曹仁商议对策"——content 描述的不是杨昂和速进兵的关系
   - 错误示例：entity1="左右", entity2="杨昂", content="许褚自称内侍"——content 和端点无关
   - **如果一段关系描述涉及的不是当前两个端点的交互，应该换用正确的端点或拆分关系**

""" + JSON_OUTPUT_ARRAY


# ============================================================
# 四、内容判断与合并（Content Judgment & Merge）
# ============================================================

JUDGE_CONTENT_NEED_UPDATE_SYSTEM_PROMPT = f"""你是一个内容比较系统。判断新版本内容是否已被旧版本内容包含。
- 新内容是旧内容的子集或重复 → false
- 新内容包含新信息、修正了旧内容、或有实质性差异 → true
- 必须同时参考每条内容附带的 source_document（来源文档）信息。来源文档是重要证据，不能忽略。
- 如果新旧内容来自不同文档，默认要更谨慎：只有在能够明确判断它们描述的是同一概念/同一关系且新内容确实只是补充时，才返回 false。
{JSON_OUTPUT_BOOL}"""

MERGE_ENTITY_NAME_SYSTEM_PROMPT = f"""你是一个实体名称合并系统。你的任务是将两个可能指向同一实体的名称合并为一个规范名称。

规则：
1. 选择一个最常用或最规范的名称作为主名称
2. 如果另一个名称是别称、简称或不同角度的描述，用括号附加在主名称后
3. 括号内的内容应该简洁，只保留关键区分信息
4. 如果两个名称差异很大但确实指向同一实体，可以用"又名"或"/"连接

示例：
- "科幻世界" + "科幻世界出版机构" = "科幻世界（出版机构）"
- "刘慈欣" + "大刘" = "刘慈欣（大刘）"
- "北京" + "北京市" = "北京"
- "基石" + "中国科幻基石丛书" = "中国科幻基石丛书（基石）"

{JSON_OUTPUT_OBJECT}
示例：{{"name": "合并后的规范名称"}}"""

JUDGE_RELATION_MATCH_SYSTEM_PROMPT = f"""你是一个关系对齐系统。判断新抽取的关系是否与已有关系相同或非常相似。
- 必须同时参考每条关系对应的 source_document。不同文档中的关系只有在明确表达的是同一对概念之间的同一语义关系时，才可视为匹配。
{JSON_OUTPUT_OBJECT}
匹配返回：{{"family_id": "...", "need_update": true/false}}
不匹配返回：null"""

MERGE_RELATION_CONTENT_SYSTEM_PROMPT = f"""你是一个内容总结系统。基于新旧两个版本的内容，重新总结生成完整准确的关系描述。

{CONTENT_MERGE_REQUIREMENTS}
- 必须参考每条关系对应的 source_document；不要把不同文档里语义不同、场景不同的关系误合并成同一条关系。
{JSON_OUTPUT_OBJECT}"""

MERGE_MULTIPLE_RELATION_CONTENTS_SYSTEM_PROMPT = f"""你是一个关系内容总结系统。基于多个关系描述，重新总结生成完整准确的关系描述。

{CONTENT_MERGE_REQUIREMENTS}
- 每条关系都可能来自不同 source_document。只有在它们确实描述同一对概念之间的同一关系时，才可以融合。"""

MERGE_MULTIPLE_ENTITY_CONTENTS_SYSTEM_PROMPT = f"""你是一个实体内容总结系统。基于多个内容描述，重新总结生成完整准确的实体描述。

{CONTENT_MERGE_REQUIREMENTS}
- 每条实体内容都可能来自不同 source_document。只有在它们确实描述同一个概念实体时，才可以融合。
{JSON_OUTPUT_OBJECT}"""


# ============================================================
# 五、知识图谱整理 - 批量与初步筛选（Knowledge Graph Organization）
# ============================================================

ANALYZE_ENTITY_CANDIDATES_PRELIMINARY_SYSTEM_PROMPT = """你是知识图谱整理系统。对候选实体进行初步筛选。

**任务**：判断哪些候选实体**可能**与当前实体有关联。这是初步筛选，不做最终决策。

**快速判断法**：
1. 名称是否相同/相似/别名？→ 放入 possible_merges
2. 是否不同对象但存在明确关联？→ 放入 possible_relations
3. 否则 → no_action

**关键**：
- 相关 ≠ 同一，content 描述是最重要依据
- 关联必须明确直接，模糊/间接/牵强的放 no_action
- 宁可 no_action 也不要误判

输出一个 ```json``` 代码块：
```json
{"possible_merges": ["family_id列表"], "possible_relations": ["family_id列表"], "no_action": ["family_id列表"]}
```"""

RESOLVE_ENTITY_CANDIDATES_BATCH_SYSTEM_PROMPT = """你是知识图谱实体对齐裁决系统。判断"当前实体"是否与某个候选实体是**完全相同的对象**。

## 三步判断流程
1. **名称匹配**：名称是否指代同一个具体对象？（别名、全称/简称可算）
2. **类型一致**：两者必须是完全相同类型（人物↔人物、地点↔地点）
3. **内容确认**：content 必须描述**完全相同的对象**

## 裁决结果
| 结果 | match_existing_id | relations_to_create | 说明 |
|------|-------------------|---------------------|------|
| match | 填写 entity_id | [] | 是同一对象 |
| match_relation | 填写 entity_id | 填写其他候选 | 是同一对象，且与其他候选有明确关联 |
| relation | 空 | 填写候选及关系 | 不是同一对象但有明确关联 |
| create_new | 空 | [] | 不合并也不建关系 |

## 合并判断（match_existing_id）
- content 描述完全相同的对象 → 合并
- 仅名称相似但 content 不同 → 不合并
- 跨文档但确实是同一具体对象 → 合并
- 概念相似但不同实例 → 不合并

## 关系判断（relations_to_create）
**重要**：候选实体是通过向量搜索检索到的，可能只是语义上相关而非真正有关联。不要因为候选出现在列表中就创建关系。

**只有**当满足以下全部条件时才创建关系：
1. 候选与当前实体描述的是完全不同的对象（排除合并可能）
2. 两者之间有**明确、直接、具体**的关联（如A是B的组成部分、A由B制造）
3. 关联可以从两个实体的 content 中直接验证

**以下情况不能创建关系**：
- 候选仅因为语义搜索被检索出来，但与当前实体无直接关联
- 两个实体只是"概念相似"或"主题相关"（如"郊外"和"城外远处"只是意思相近）
- 无法从 content 验证的模糊关联

### match vs match_relation
- match：只需合并，其他候选不需要建关系
- match_relation：合并 + 还有其他候选确实需要建关系
- **大多数匹配只需 match**，match_relation 仅用于罕见的多重关联场景

只输出一个 ```json ... ``` 代码块：
```json
{
  "match_existing_id": "",
  "update_mode": "reuse_existing | merge_into_latest | create_new",
  "merged_name": "",
  "merged_content": "",
  "relations_to_create": [],
  "confidence": 0.0
}
```"""

JUDGE_NEED_CREATE_RELATION_SYSTEM_PROMPT = f"""你是一个关系判断系统。根据两个实体的信息，判断它们之间是否存在明确的、有意义的关联。

{RELATION_VALIDITY_CRITERIA}
{JSON_OUTPUT_OBJECT}
代码块内部格式：{{"need_create": true/false}}"""

GENERATE_RELATION_CONTENT_SYSTEM_PROMPT = f"""你是一个知识图谱关系生成系统。根据两个实体的信息和记忆缓存，生成准确完整的关系描述。
关系描述只专注两个实体之间的关系，不包含实体自身的其他信息。
{JSON_OUTPUT_OBJECT}"""


# ============================================================
# 六、知识图谱整理 - 精细化判断（Detailed Judgment）
# ============================================================

def analyze_entity_pair_detailed_system_prompt(existing_relations_note: str = "") -> str:
    """生成 analyze_entity_pair_detailed 的 system_prompt"""
    return f"""你是一个知识图谱整理系统。你的任务是对两个实体进行**精细化判断**。

{ENTITY_ALIGNMENT_CORE_PRINCIPLES}

{DETAILED_JUDGMENT_CRITERIA}
{existing_relations_note}
只输出一个 ```json ... ``` 代码块，不要包含任何其他文字。代码块内部格式：
{{
  "action": "merge" | "create_relation" | "no_action",
  "reason": "详细的判断理由",
  "relation_content": "如果action是create_relation，填写关系描述；否则留空",
  "merge_target": "如果action是merge，填写应该保留的family_id（版本数多的）；否则留空"
}}"""

RESOLVE_RELATION_PAIR_BATCH_SYSTEM_PROMPT = """你是关系对齐裁决系统。判断同一实体对的新关系是否与某个已有关系描述的是**同一性质的关系**。

## 核心判断规则

### 应该匹配（match_existing）
两个关系描述的是**同一性质/同一类型的关联**：
- "弓是弓矢的组成部分" ↔ "弓矢由弓和箭组成" → 匹配（都是组成关系）
- "同属龙王统治的水域" ↔ "是水族居住的地方" → 匹配（都是属地关系）
- "史进被邀请参加宴席" ↔ "史进为庆祝中秋节准备酒食筵宴" → 匹配（都是关于宴席参与）
- "太宗率文武" ↔ "太宗带领文武多官" → 匹配（都是率领关系）

### 不应匹配（create_new）
两个关系虽然涉及同一实体对，但描述的是**不同性质的关联**：
- "在酒店休息避寒" vs "在酒店喝酒写诗" → 不匹配（休息 vs 喝酒是不同行为）
- "父子情谊" vs "一起参加赠礼事件" → 不匹配（情感关系 vs 具体事件）
- "修炼成仙的方式" vs "成仙是仙的状态" → 不匹配（方法 vs 状态，语义不同）
- "看到戴毡笠的人" vs "自己戴毡笠" → 不匹配（看 vs 穿戴，主体不同）
- "情感对文武的影响" vs "聚集文武" → 不匹配（影响关系 vs 集合行为）

## 判断步骤
1. 提取新关系的**核心谓语/动作**是什么
2. 提取已有关系的**核心谓语/动作**是什么
3. 如果核心谓语是同一性质 → match_existing
4. 如果核心谓语是不同性质 → create_new
5. 不要因为"都涉及同一实体对"或"都在相似场景"就匹配

## confidence 规则
- 确信匹配：0.8-1.0
- 确信不匹配：0.7-0.9
- 不确定：0.3-0.6

只输出一个 ```json``` 代码块：
```json
{
  "action": "match_existing | create_new",
  "matched_relation_id": "匹配则填 relation_id，否则空字符串",
  "need_update": false,
  "merged_content": "需要更新时给出最终内容，否则空字符串",
  "confidence": 0.0
}
```"""


