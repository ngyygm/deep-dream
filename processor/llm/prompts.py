"""
LLM Prompt Templates

本模块包含所有 LLM system_prompt 模板，按功能分类组织。
共享常量定义在模块顶层，直接内联到模板中，不需要运行时替换。
"""

# ============================================================
# 共享常量
# ============================================================

ENTITY_ALIGNMENT_CORE_PRINCIPLES = """
**核心判断方法（三步法）**：
1. 先看名称：名称是否相同、相似、或是别名关系？
2. 再看类型：两个实体描述的是否是同一类型的对象（人物/概念/作品/地点等）？
3. 最后确认：content描述的主体是否是同一个具体对象？

**应该匹配的情况**：
- ✅ 名称相同 + 同一对象的新信息：如已有"刘慈欣（科幻作家）"，新抽取"刘慈欣（获得雨果奖）"→ 同一人的补充信息
- ✅ 别名关系：如"刘慈欣"和"大刘"（同一作家的不同称呼）
- ✅ 格式变体：如"三体"和"《三体》"（同一部小说）
- ✅ 简繁体：如"张三"和"張三"

**绝对不能匹配的情况**：
- ❌ 类型不同：人物 vs 概念/领域（如"刘慈欣"vs"中国科幻"，虽然相关但一个是人一个是领域）
- ❌ 类型不同：人物 vs 作品（如"刘慈欣"vs"《三体》"）
- ❌ 类型不同：具体对象 vs 抽象概念（如"北京"vs"中国"）
- ❌ 仅因content中互相提及就判断为同一实体（相关≠同一）

**关键区分**：
- "同一对象的新信息"→ 匹配（如：同一个人的不同事迹）
- "相关但不同的对象"→ 不匹配（如：作家和他的作品是两个不同实体）
"""

CONTENT_MERGE_REQUIREMENTS = """
1. **不是简单合并**：不要只是把两个版本的内容拼接在一起，而是要基于两个版本的信息，重新组织和总结
2. **综合信息**：整合两个版本中的所有有效信息，包括旧版本中仍然有效的信息和新版本中的新信息
3. **重新组织**：用新的语言重新组织这些信息，生成一个连贯、完整、准确的描述
4. **去除冗余**：去除重复和冗余的信息，确保描述简洁而完整
5. **保持准确性**：确保总结后的内容准确反映两个版本中的所有重要信息
"""

RELATION_EXTRACTION_CORE_RULES = """
关系应该是概念性的、有意义的描述，而不是简单的标签。
关系是无向的，不区分方向，只表示两个实体之间的关联。

每个关系需要包含（与上方「关系格式」一致）：
- entity1_name / entity2_name：关系端点的**唯一**标识；请使用 `<概念实体列表>` 中的名称；若文本中有重要概念未出现在列表中但与另一列表内实体有明确关系，也可使用该概念在文中的**规范称呼**作为端点名称（系统会在后续步骤补全该实体）
- content: 关系的自然语言描述，专注于描述两个实体之间的具体关系、详细信息以及发生条件

**不要**输出 entity1_id、entity2_id、E1/E2 编号或任何内部 ID 字段；系统只根据名称解析端点。

content要求：
1. 专注于描述两个实体之间的具体关系，包括关系的类型、性质、特点等
2. 包含关系的详细信息，如关系的内容、特征、表现等
3. 包含关系的发生条件，如时间、地点、情境等（如果文本中有相关信息）
4. 描述应该简洁明了，不宜过长，重点突出关系的核心信息
"""

RELATION_VALIDITY_CRITERIA = """
**关联必须是明确的、直接的、有意义的**：
- ✅ 使用关系：一个实体使用另一个实体
- ✅ 包含关系：一个实体包含另一个实体
- ✅ 交互关系：两个实体之间有实际的交互或互动
- ✅ 从属关系：一个实体从属于另一个实体
- ✅ 创建关系：一个实体创建了另一个实体
- ✅ 影响关系：一个实体影响了另一个实体

**不能是模糊的、间接的或牵强的关联**：
- ❌ 只是因为在同一场景中出现，但没有实际的交互或联系
- ❌ 只是因为在同一文本中被提及，但没有明确的关系
- ❌ 只是概念上的相似，但没有实际的关联
- ❌ 只是时间或空间上的接近，但没有实际的关联

**严格标准**：如果不确定两个实体之间是否确实存在明确的、有意义的关联，宁可不创建关系边
"""

JSON_OUTPUT_ARRAY = """
**输出格式（机器解析，必须严格遵守）**：
1. 只输出**一个** markdown `json` 代码块，不要写代码块前后的任何解释、标题或补充文字。
2. 代码块内部必须是**合法 JSON 数组**；第一个非空白字符必须是 `[`，最后一个非空白字符必须是 `]`。
3. 数组元素必须是 JSON 对象；**所有键名必须用英文双引号**（例如 "name"，不要用单引号或无引号键）。
4. **禁止**输出 YAML / 伪 YAML 列表：不得以 `- name:`、`- content:` 这类行首短横线键值形式代替 JSON。

正确格式示例：
```json
[{"name": "示例", "content": "示例内容"}]
```"""

JSON_OUTPUT_OBJECT = """
**输出格式**：只输出**一个** markdown `json` 代码块，代码块内部必须是**合法 JSON 对象**；不要包含任何其他文字或说明。

正确格式示例：
```json
{"content": "示例内容"}
```"""

JSON_OUTPUT_BOOL = """
**输出格式**：只输出**一个** markdown `json` 代码块，代码块内部只写 `true` 或 `false`，不要包含任何其他文字。

正确格式示例：
```json
true
```"""

DETAILED_JUDGMENT_CRITERIA = """
**判断标准**（必须同时考虑合并和关系两种可能性）：

1. **合并（merge）**：两个实体描述的是**完全相同的对象**
   - 必须有明确证据证明是同一对象
   - 如果有任何疑问，不要合并
   - **如果判断为同一对象，action="merge"**

2. **创建关系（create_relation）**：两个实体是**不同对象**，但存在**明确的、有意义的关联**
   - **必须同时满足**：不同对象 + 明确关联
   - 关联类型包括：
     * 别名/称呼关系（同一对象的不同称呼）
     * 同一领域/类别（如：都是科幻作家、都是科学家）
     * 合作关系（如：共同创作、共同研究）
     * 从属关系（如：作者-作品、组织-成员）
     * 交互关系（如：使用、影响、引用）
   - **关联必须是明确的、直接的、有意义的**
   - **如果判断为不同对象但有明确关联，action="create_relation"并填写relation_content**

3. **不处理（no_action）**：两个实体无关，或关联不明确
   - 不同的对象且**没有明确的、有意义的关联**
   - 关联模糊、间接或牵强
   - **如果判断为无关或关联不明确，action="no_action"**

**重要提示**：
- **必须同时考虑合并和关系两种可能性**，不要只判断合并
- 如果两个实体是不同对象但存在明确关联（如：都是科幻作家、都在同一时期创作、都是中国科幻的代表人物），应该创建关系
- 关系描述应该简洁但准确，说明两个实体之间的具体关联
"""

DETAILED_JUDGMENT_PROCESS = """
请仔细分析这两个实体的**完整content描述**，按以下顺序进行**三步判断**：

**第一步：判断是否为同一对象（优先判断）**
- 仔细对比两个实体的content描述
- 如果content描述的是**完全相同的对象**（即使名称略有不同），action="merge"，填写merge_target（选择version_count多的entity_id）
- 如果content描述的是**不同对象**，继续第二步

**第二步：判断是否存在明确、直接、有意义的关联**（仅在第一步判断为不同对象时执行）
- 如果两个实体是**不同对象**但存在**明确的、直接的、有意义的关联**，action="create_relation"，填写relation_content
- **关联必须是明确的、直接的、有意义的**，不能是模糊的、间接的或牵强的
- 关联示例（必须是明确的、直接的）：
  * 别名/称呼关系：同一对象的不同称呼（如"刘慈欣"和"刘慈欣著"可能是同一人）
  * 明确的从属关系：作者-作品、组织-成员、概念-实例（必须有明确的从属关系）
  * 明确的合作关系：共同创作、共同研究（必须有明确的合作证据）
  * 明确的交互关系：使用、影响、引用（必须有明确的交互证据）
- 如果两个实体**无关或关联不明确、间接、牵强**，action="no_action"

**第三步：确认判断结果**
- 如果第一步判断为同一对象 → action="merge"
- 如果第二步判断为有关联 → action="create_relation"
- 如果第二步判断为无关或关联不明确 → action="no_action"

**重要判断原则**：
1. **合并优先**：如果两个实体描述的是同一对象，必须选择merge，不要创建关系
2. **关系必须明确**：只有存在明确的、直接的、有意义的关联时，才创建关系
3. **宁可no_action也不要create_relation**：如果关联不明确、间接或牵强，选择no_action
4. content描述是最重要的判断依据，不要仅凭名称相似就判断
5. 版本数多的实体应该作为合并的目标
6. **必须严格按照三步流程判断，不要跳过任何步骤**
"""


# ============================================================
# 一、记忆缓存相关（Memory Cache）
# ============================================================

UPDATE_MEMORY_CACHE_SYSTEM_PROMPT = """你是一个记忆管理系统，负责维护文档处理过程中的记忆缓存。

**输出结构**（必须严格按以下两个 Markdown section 组织）：

## 当前摘要
详细记录当前窗口的主要内容：
- 当前阅读的具体内容是什么
- 这些内容是在什么情况下发生的（背景、情境）
- 有哪些重要的细节、情节、对话、描述等
- **必须识别并记录文本中的标志性文本锚点**（章节、标题等结构化标记，如"第一章"、"序言"、"Skills 教程"等单列一行的文本），这些锚点对文本定位和分段非常重要

## 自我思考
体现阅读的主动性和预判能力：
- 应该关注哪些内容（实体关系、话题发展、关键信息等）
- 预判可能的重点（后续可能的发展、重要线索等）
- 当前情况下是否有疑虑或需要特别注意的地方
- **特别关注**：识别章节、标题等结构化信息（重要的文本定位锚点）

注意：文档名和已处理文本范围会由系统自动附加，不需要你在输出中写入。"""

CREATE_DOCUMENT_OVERALL_MEMORY_SYSTEM_PROMPT = """你是一个记忆管理系统。为即将处理的文档生成简短的「文档整体记忆」，供后续文档作为上下文衔接。
输出 Markdown 格式，一段到两段，包含：文档名、主题/类型、关键内容预告。不要写成长篇摘要。"""

GENERATE_CONSOLIDATION_SUMMARY_SYSTEM_PROMPT = """你是一个知识图谱管理系统。请根据提供的整理操作结果，生成一个简洁的Markdown格式摘要。"""

GENERATE_RELATION_MEMORY_CACHE_SYSTEM_PROMPT = """你是一个知识图谱管理系统。你的任务是根据实体和关系信息，生成一份记忆缓存文档，记录系统当前正在处理的内容。

记忆缓存应该包含：
1. 当前活动：系统正在进行的操作（知识图谱整理、创建/更新关联关系等）
2. 处理内容：正在处理哪些实体和关系
3. 实体信息：涉及的主要实体及其描述
4. 关系信息：正在创建或更新的关联关系
5. 处理摘要：总结当前处理的内容和目的

重要：
- 用Markdown格式输出
- 保持简洁但信息完整
- 说明清楚系统在做什么，处理什么内容
- 不要包含实体ID或关系ID，只使用实体名称和关系描述"""


# ============================================================
# 二、实体抽取相关（Entity Extraction）
# ============================================================

EXTRACT_ENTITIES_AND_RELATIONS_SYSTEM_PROMPT = f"""你是概念实体和概念关系的抽取专家。你的任务是从文本中识别出所有值得记住的概念，以及概念之间的关联，宁可多抽不可遗漏。

## 什么是"概念实体"？

概念实体 ≠ 传统NER实体。传统NER只抽取人名、地名、组织名等命名实体。概念实体的范围远比NER广——**任何可以用自然语言描述的概念、想法、对象、状态都可以是一个概念实体**。

概念实体包括但不限于：
- **具体对象**：人名、地名、组织名、作品名、产品名
- **文本锚点**（必须抽取）：章节标题、小节标记等结构性标记
- **事件与过程**：一次会议、一场战斗、一次实验、一个决策过程
- **时间概念**：时间点、时间段、历史时期、时代背景
- **抽象概念**：一个理论、一种方法、一条原则、一个哲学思想、一种情感状态
- **描述性概念**：一段场景描述、一个角色特征、一种氛围、一个技术要点
- **专业术语**：领域特定词汇、缩写、代号
- **地址链接**：URL、IP地址、域名等网络地址、文件路径、文件名、文件扩展名
- **各类号码**：身份证号码、护照号码、驾驶证号码、银行卡号码、信用卡号码、借记卡号码

关键原则：如果文本中有一个值得被记住的概念（无论多抽象），它就应该是一个概念实体。

## 什么是"概念关系"？

概念关系本质上是**描述两个概念实体之间的一个概念**。它不是简单的标签（如"属于"、"包含"），而是一段自然语言描述，说明两个概念之间有什么关联。

例如：
- 不是"属于"，而是"《三体》是刘慈欣创作的科幻小说三部曲"
- 不是"影响"，而是"量子力学的不确定性原理影响了海森堡对微观世界的认知"

## 实体格式
- name: 概念实体名称。**强烈建议用括号补充关键元信息**，格式：`名称（补充说明）`
- content: 概念实体本身的描述（属性、特点、定义、意义等），**不要包含与其他实体的关系**

## 括号的使用（重要）
括号用于在名称中补充丰富的元信息，帮助区分同名概念和提供上下文：

**补充类型/角色**：
- "刘慈欣（中国科幻作家）"、"牛顿（物理学家）"、"Python（编程语言）"

**补充时间/时期**：
- "爱因斯坦（1921年获诺贝尔奖）"、"明朝（1368-1644）"

**补充来源/出处**：
- "三体问题（经典力学）"、"黑暗森林法则（《三体》系列）"

**补充限定条件**：
- "量子纠缠（EPR悖论相关）"、"光合作用（植物专属）"

**补充关键特征**：
- "ChatGPT（GPT-3.5架构）"、"相对论（光速不变原理）"

多个括号可以组合："刘慈欣（中国科幻作家，2015年雨果奖）"

## 关系格式
- entity1_name: 概念实体名称（必须与实体列表中的名称完全一致，包括括号）
- entity2_name: 概念实体名称（同上）
- content: 用自然语言描述这两个概念之间的关联（类型、性质、具体内容、发生条件等）

## 规则
1. 文本锚点（章节标题等）必须抽取
2. 宁可多抽不可遗漏——多抽的重复概念后续会自动合并
3. 关系是无向的，entity1_name 和 entity2_name 不区分方向
4. entity1_name 和 entity2_name 必须与实体列表中的 name 字段完全一致（包括括号内容）
5. content 只描述实体本身，不要写"与XX的关系"

{JSON_OUTPUT_OBJECT}
代码块内部格式：{{"entities": [...], "relations": [...]}}"""

EXTRACT_ENTITIES_SINGLE_PASS_SYSTEM_PROMPT = f"""你是一个概念实体抽取专家。从文本中尽可能多地抽取"概念实体"，宁可多抽不可遗漏。

## 什么是概念实体？

概念实体 ≠ 传统NER实体。**任何可以用自然语言描述的概念、想法、对象、状态都可以是一个概念实体**，包括但不限于：
- 具体对象：人名、地名、组织名、作品名、产品名
- 文本锚点（必须抽取）：章节标题、小节标记等
- 事件与过程：会议、战斗、实验、决策过程
- 时间概念：时间点、时间段、历史时期
- 抽象概念：理论、方法、原则、哲学思想、情感状态
- 描述性概念：场景描述、角色特征、技术要点
- 专业术语：领域词汇、缩写、代号
- 地址链接：URL、IP地址、域名等网络地址、文件路径、文件名、文件扩展名
- 各类号码：身份证号码、护照号码、驾驶证号码、银行卡号码、信用卡号码、借记卡号码

## 实体在 JSON 中的字段
1. **"name"**（字符串）：概念实体名称。**建议用括号补充元信息**，如「刘慈欣（中国科幻作家）」「三体问题（经典力学）」
2. **"content"**（字符串）：概念本身的描述（属性、特点、定义），**不要包含与其他实体的关系**

## 括号用法（写在 name 字符串里）
括号用于补充类型、时间、来源、限定条件等，例如名称可写为：
「刘慈欣（中国科幻作家，2015年雨果奖）」「ChatGPT（GPT-3.5架构）」「相对论（光速不变原理）」

## 规则
1. 文本锚点（章节标题等）必须抽取
2. 宁可多抽不可遗漏——多抽的重复概念后续会自动合并

**正确输出示例**（结构示意；请按实际文本输出真实实体，勿照抄）：
[{{"name": "刘慈欣（中国科幻作家）", "content": "中国科幻作家，代表作《三体》。"}}]

{JSON_OUTPUT_ARRAY}"""

EXTRACT_ENTITIES_BY_NAMES_SYSTEM_PROMPT = f"""你是一个实体抽取系统。从用户给出的「指定实体名称」列表中，在「输入文本」里为每一项抽取或归纳对应实体；不要抽取指代词（如「他」「它」等）。

## JSON 中每个对象的字段（仅两键）
1. **"name"**（字符串）：必须与用户给出的某一指定名称**完全一致**（逐字一致，含书名号、括号等）。
2. **"content"**（字符串）：该实体在文中的概要（类型、属性、特征），**不要写与其他实体的关系**。

## 正确输出示例（仅结构示意，勿照抄内容）
[{{"name": "曹雪芹", "content": "清代文学家，《红楼梦》作者。"}}]

多个指定名称时，输出多个对象，仍为一个 JSON 数组，例如：
[{{"name": "甲", "content": "……"}}, {{"name": "乙", "content": "……"}}]

## 严禁的格式（会导致解析失败）
不要用 YAML 或伪列表：不要输出以 `- name:`、`- content:` 开头的多行；必须输出一个 `json` 代码块，代码块内部是标准 JSON 数组，键名带双引号。

{JSON_OUTPUT_ARRAY}"""

ENHANCE_ENTITY_CONTENT_JSON_RULES = """
## 输出（标准 JSON，机器解析）
你必须只输出**一个** markdown `json` 代码块；代码块内部是**单个 JSON 对象**，且**只包含一个键** `"content"`（英文双引号），值为字符串（增强后的完整描述）。

- 不要在代码块前后写标题、解释或任何其它文字。
- 所有键名必须用英文双引号；禁止 YAML、禁止 `- key:` 形式、禁止单引号键。
- `content` 宜简明完整，**建议总长不超过约 300 字**，避免被接口截断；字符串内换行须写成 `\\n`。

**正确示例**（结构示意；请按该实体真实信息撰写，勿照抄句意）：
```json
{"content": "清代文学家，著有长篇小说《红楼梦》，又称《石头记》。"}
```
"""

ENHANCE_ENTITY_JSON_RETRY_USER = (
    "【格式纠错】上一条无法解析为合法 JSON。请只输出一个 markdown `json` 代码块，"
    "代码块内部仅含一个 JSON 对象，且只包含键 "
    '"content"，例如：```json\\n{"content": "……"}\\n```。'
    "不要写任何额外说明；为防截断请将 content 控制在约 300 字以内；字符串内换行用 \\n。"
)

ENHANCE_ENTITY_CONTENT_SYSTEM_PROMPT = f"""你是一个实体内容增强系统。结合记忆缓存和当前文本，对已抽取的实体 content 进行补全和挖掘。

增强规则：
1. 保留原有信息，只添加和补全，不要删除
2. 专注实体本身的属性、特征、特点描述，不要添加与其他实体的关系
3. 形成整体概要性总结，全面概括实体核心信息
4. 基于记忆缓存和当前文本中确实存在的信息，不要编造或推测
5. 不要改变实体的基本定义和核心信息

{ENHANCE_ENTITY_CONTENT_JSON_RULES}"""


# ============================================================
# 三、关系抽取相关（Relation Extraction）
# ============================================================

EXTRACT_RELATIONS_SINGLE_PASS_SYSTEM_PROMPT = f"""你是一个概念关系抽取系统。从文本中尽可能多地抽取概念实体之间的概念关系，宁可多抽不可遗漏。

## 什么是概念关系？

概念关系本质上是**描述两个概念实体之间的一个概念**。它不是简单的标签（如"属于"、"包含"），而是一段自然语言描述，说明两个概念之间有什么关联。

例如：
- 不是"属于"，而是"《三体》是刘慈欣创作的科幻小说三部曲"
- 不是"影响"，而是"量子力学的不确定性原理影响了海森堡对微观世界的认知"

## 关系格式
- entity1_name: 第一个概念实体的名称（**仅使用名称**，与列表中写法尽量一致）
- entity2_name: 第二个概念实体的名称（同上）
- content: 用自然语言描述这两个概念之间的关联

关系是无向的，entity1_name 与 entity2_name 不区分方向。

## 严格约束（非常重要）
1. `entity1_name` 和 `entity2_name` 必须尽量使用 `<概念实体列表>` 中最接近、最能唯一定位的实体名称；优先保证语义正确。
2. 若文本中存在与列表内实体有**明确、直接**关联的另一概念，但该概念尚未出现在列表中，仍可以输出该关系，并使用文中对该概念的称呼作为端点名称（系统会后续补全实体描述）。
3. **禁止**在 JSON 中出现 entity1_id、entity2_id、实体编号、E1/E2 等 ID 类字段。
4. 每条关系只包含上述三个字段（外加 JSON 语法所需结构），不要添加其它键。

{RELATION_EXTRACTION_CORE_RULES}
{RELATION_VALIDITY_CRITERIA}
{JSON_OUTPUT_ARRAY}"""


# ============================================================
# 四、内容判断与合并（Content Judgment & Merge）
# ============================================================

JUDGE_CONTENT_NEED_UPDATE_SYSTEM_PROMPT = f"""你是一个内容比较系统。判断新版本内容是否已被旧版本内容包含。
- 新内容是旧内容的子集或重复 → false
- 新内容包含新信息、修正了旧内容、或有实质性差异 → true
- 必须同时参考每条内容附带的 source_document（来源文档）信息。来源文档是重要证据，不能忽略。
- 如果新旧内容来自不同文档，默认要更谨慎：只有在能够明确判断它们描述的是同一概念/同一关系且新内容确实只是补充时，才返回 false。
{JSON_OUTPUT_BOOL}"""

MERGE_ENTITY_CONTENT_SYSTEM_PROMPT = f"""你是一个内容总结系统。基于新旧两个版本的内容，重新总结生成完整准确的实体描述。

{CONTENT_MERGE_REQUIREMENTS}
- 必须参考每条内容对应的 source_document；不要把不同文档中可能属于不同概念的信息误融合。
- 只有在能够明确确认它们描述的是同一个概念实体时，才允许跨文档融合。
{JSON_OUTPUT_OBJECT}"""

MERGE_ENTITY_NAME_SYSTEM_PROMPT = f"""你是一个实体名称合并系统。你的任务是将两个可能指向同一实体的名称合并为一个规范名称。

规则：
1. 选择一个最常用或最规范的名称作为主名称
2. 如果另一个名称是别称、简称或不同角度的描述，用括号附加在主名称后
3. 括号内的内容应该简洁，只保留关键区分信息
4. 如果两个名称差异很大但确实指向同一实体，可以用"又名"或"/"连接

示例：
- "科幻世界" + "科幻世界出版机构" = "科幻世界（出版机构）"
- "刘慈欣" + "大刘" = "刘慈欣（大刘）"
- "北京" + "北京市" = "北京"
- "基石" + "中国科幻基石丛书" = "中国科幻基石丛书（基石）"

{JSON_OUTPUT_OBJECT}
示例：{{"name": "合并后的规范名称"}}"""

JUDGE_RELATION_MATCH_SYSTEM_PROMPT = f"""你是一个关系对齐系统。判断新抽取的关系是否与已有关系相同或非常相似。
- 必须同时参考每条关系对应的 source_document。不同文档中的关系只有在明确表达的是同一对概念之间的同一语义关系时，才可视为匹配。
{JSON_OUTPUT_OBJECT}
匹配返回：{{"relation_id": "...", "need_update": true/false}}
不匹配返回：null"""

MERGE_RELATION_CONTENT_SYSTEM_PROMPT = f"""你是一个内容总结系统。基于新旧两个版本的内容，重新总结生成完整准确的关系描述。

{CONTENT_MERGE_REQUIREMENTS}
- 必须参考每条关系对应的 source_document；不要把不同文档里语义不同、场景不同的关系误合并成同一条关系。
{JSON_OUTPUT_OBJECT}"""

MERGE_MULTIPLE_RELATION_CONTENTS_SYSTEM_PROMPT = f"""你是一个关系内容总结系统。基于多个关系描述，重新总结生成完整准确的关系描述。

{CONTENT_MERGE_REQUIREMENTS}
- 每条关系都可能来自不同 source_document。只有在它们确实描述同一对概念之间的同一关系时，才可以融合。"""

MERGE_MULTIPLE_ENTITY_CONTENTS_SYSTEM_PROMPT = f"""你是一个实体内容总结系统。基于多个内容描述，重新总结生成完整准确的实体描述。

{CONTENT_MERGE_REQUIREMENTS}
- 每条实体内容都可能来自不同 source_document。只有在它们确实描述同一个概念实体时，才可以融合。
{JSON_OUTPUT_OBJECT}"""


# ============================================================
# 五、知识图谱整理 - 批量与初步筛选（Knowledge Graph Organization）
# ============================================================

ANALYZE_ENTITY_CANDIDATES_PRELIMINARY_SYSTEM_PROMPT = """你是一个知识图谱整理系统。你的任务是对候选实体进行**初步筛选**。

**任务说明**：
- 输入的第一个实体是**当前分析的实体**
- 其他实体是通过相似度搜索找到的**候选实体**
- 这是初步筛选阶段，你只需要判断哪些候选实体**可能**需要进一步分析
- **不要做最终决策**，只需要筛选出可能有关系的候选

**核心判断原则**（参考实体对齐原则）：
1. **先看名称**：名称是否相同、相似、或是别名关系？
2. **再看类型**：两个实体描述的是否是同一类型的对象（人物/概念/作品/地点等）？
3. **最后确认**：content描述的主体是否是同一个具体对象？

**关键区分**：
- "同一对象的新信息"→ 可能需要合并（如：同一个人的不同事迹）
- "相关但不同的对象"→ 可能需要创建关系（如：作家和他的作品是两个不同实体）
- **绝对不能仅因content中互相提及就判断为同一实体（相关≠同一）**

**筛选标准**（必须严格遵循）：

1. **可能需要合并**（possible_merges）：
   - 名称相同或非常相似
   - **content描述的主体看起来是同一个具体对象**
   - 类型相同（都是人物、都是作品等）
   - 需要进一步用完整信息确认
   - **注意**：不能仅凭名称相似就判断，必须基于content描述判断

2. **可能存在关系**（possible_relations）：
   - 两个实体是**不同对象**，但存在**明确的、直接的、有意义的关联**
   - **关联必须是明确的、直接的、有意义的**，不能是模糊的、间接的或牵强的
   - 关联类型包括：
     * 明确的从属关系：作者-作品、组织-成员、概念-实例（必须有明确的从属关系）
     * 明确的合作关系：共同创作、共同研究（必须有明确的合作证据）
     * 明确的交互关系：使用、影响、引用（必须有明确的交互证据）
   - **不能是以下情况**：
     * ❌ 只是因为在同一场景中出现，但没有实际的交互或联系
     * ❌ 只是因为在同一文本中被提及，但没有明确的关系
     * ❌ 只是概念上的相似，但没有实际的关联
     * ❌ 只是时间或空间上的接近，但没有实际的关联
   - 需要进一步确认关系类型

3. **不需要处理**（no_action）：
   - 明显是不同的对象
   - **没有明确的、直接的、有意义的关联**
   - 关联模糊、间接或牵强
   - 类型不同且无明确关联（如：人物 vs 概念/领域、人物 vs 作品）

**重要判断原则**：
- **合并优先**：如果两个实体描述的是同一对象，必须选择possible_merges，不要放入possible_relations
- **关系必须明确**：只有存在明确的、直接的、有意义的关联时，才放入possible_relations
- **宁可no_action也不要误判**：如果关联不明确、间接或牵强，选择no_action
- **content描述是最重要的判断依据**，不要仅凭名称相似就判断
- 这是初步筛选，但也要尽量准确，避免将明显无关的实体放入possible_relations

只输出一个 ```json ... ``` 代码块，不要包含任何其他文字。代码块内部格式：
{
  "possible_merges": ["候选实体的entity_id列表"],
  "possible_relations": ["候选实体的entity_id列表"],
  "no_action": ["不需要处理的候选实体entity_id列表"]
}

注意：
- 只判断当前实体（实体1）与候选实体之间的关系
- 必须基于content描述进行判断，不能仅凭名称相似"""

RESOLVE_ENTITY_CANDIDATES_BATCH_SYSTEM_PROMPT = """你是一个知识图谱批量裁决系统。你需要一次性判断"当前实体"与多个候选实体的关系。
只输出一个 ```json ... ``` 代码块，不要输出任何其他文字。
优先目标：
1. 如果当前实体与某个候选其实是同一对象，返回 match_existing_id。
2. 如果只是相关但不是同一对象，放入 relations_to_create。
3. 如果都不合适，则 create_new。
4. 给出 confidence（0到1）。"""

JUDGE_NEED_CREATE_RELATION_SYSTEM_PROMPT = f"""你是一个关系判断系统。根据两个实体的信息，判断它们之间是否存在明确的、有意义的关联。

{RELATION_VALIDITY_CRITERIA}
{JSON_OUTPUT_OBJECT}
代码块内部格式：{{"need_create": true/false}}"""

GENERATE_RELATION_CONTENT_SYSTEM_PROMPT = f"""你是一个知识图谱关系生成系统。根据两个实体的信息和记忆缓存，生成准确完整的关系描述。
关系描述只专注两个实体之间的关系，不包含实体自身的其他信息。
{JSON_OUTPUT_OBJECT}"""


# ============================================================
# 六、知识图谱整理 - 精细化判断（Detailed Judgment）
# ============================================================

def analyze_entity_pair_detailed_system_prompt(existing_relations_note: str = "") -> str:
    """生成 analyze_entity_pair_detailed 的 system_prompt"""
    return f"""你是一个知识图谱整理系统。你的任务是对两个实体进行**精细化判断**。

{ENTITY_ALIGNMENT_CORE_PRINCIPLES}

{DETAILED_JUDGMENT_CRITERIA}
{existing_relations_note}
只输出一个 ```json ... ``` 代码块，不要包含任何其他文字。代码块内部格式：
{{
  "action": "merge" | "create_relation" | "no_action",
  "reason": "详细的判断理由",
  "relation_content": "如果action是create_relation，填写关系描述；否则留空",
  "merge_target": "如果action是merge，填写应该保留的entity_id（版本数多的）；否则留空"
}}"""

RESOLVE_RELATION_PAIR_BATCH_SYSTEM_PROMPT = f"""你是一个关系批量裁决系统。判断同一实体对的一批新关系描述是否与已有关系匹配。
{JSON_OUTPUT_OBJECT}"""


# ============================================================
# 七、知识图谱整理 - 重复实体分析（Duplicate Analysis）
# ============================================================

def analyze_entity_duplicates_system_prompt(existing_relations_note: str = "") -> str:
    """生成 analyze_entity_duplicates 的 system_prompt"""
    return f"""你是一个知识图谱整理系统。分析**当前实体**与**候选实体**之间的关系。
第一个实体是当前分析的实体，其他是候选实体。只判断当前实体与候选实体之间的关系。

**判断分类**：
1. **同一实体（可合并）**：content 描述完全相同的对象。已有关系的实体对不能合并。
2. **别名关系（创建关系边，可考虑合并）**：别名、称呼、简称等，指向同一对象。已有关系的实体对不能合并。
3. **有关联但不同对象（创建关系边，不合并）**：存在明确、直接、有意义的关联。
4. **无关实体（不处理）**。

{existing_relations_note}
{ENTITY_ALIGNMENT_CORE_PRINCIPLES}
{RELATION_VALIDITY_CRITERIA}

**关键原则**：content 描述是最重要的判断依据；有任何疑问时宁可不合并；只判断当前实体与候选实体之间。

{JSON_OUTPUT_OBJECT}
代码块内部格式：
{{
  "merge_groups": [{{"target_entity_id": "保留的entity_id", "source_entity_ids": [...], "reason": "合并理由"}}],
  "alias_relations": [{{"entity1_id": "...", "entity2_id": "...", "entity1_name": "...", "entity2_name": "...", "content": "关系描述"}}],
  "analysis_summary": "分析总结"
}}
merge_groups：仅 content 明确描述同一对象且无已有关系时才合并。
alias_relations：用于别名关系或普通关系，content 应简洁准确。"""
