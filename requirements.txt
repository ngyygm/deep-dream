# Temporal Memory Graph 依赖包

# 数据库
# SQLite3 是Python标准库，无需安装

# LLM 客户端（OpenAI/GLM 等兼容接口使用 openai SDK；Ollama 原生 /api/chat 由项目内代码直接调用）
openai>=1.0.0

# 如果需要使用其他LLM服务，可以添加：
# anthropic>=0.3.0  # Claude
# google-generativeai>=0.3.0  # Gemini

# Embedding模型（可选，用于改进实体搜索）
# 如果使用embedding进行相似度搜索，需要安装：
sentence-transformers>=2.2.0
# Qwen3-Embedding 等需 transformers>=4.51；与 sentence-transformers 兼容请保持 <5
transformers>=4.51.0,<5.0.0
pillow>=9.1.0
# 如果需要GPU加速，可以安装：
# torch>=2.0.0  # PyTorch（通常sentence-transformers会自动安装）

# Neo4j 图数据库驱动（可选，用于 Neo4j 后端）
neo4j>=5.0

# 向量搜索（可选，用于大规模相似度搜索）
sqlite-vec>=0.1.6

# 工具库
python-dateutil>=2.8.0
numpy>=1.21.0  # 用于embedding计算

# 可视化库（可选，用于图谱可视化）
pyvis>=0.3.0  # 交互式 HTML 可视化
networkx>=3.0.0  # 图结构处理和静态可视化
matplotlib>=3.5.0  # 静态图片可视化
