"""Route modules for the Deep-Dream API.

Each module owns a focused set of HTTP route handlers. Internally Flask still
uses Blueprint objects for registration, but the package name stays product-level
and readable: routes.
"""
